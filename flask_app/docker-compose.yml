# version: '3.8'

services:
  mydb:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: my-secret-pw
      MYSQL_DATABASE: mysql
    ports:
      - "3306:3306"

  web:
    build: .
    ports:
      - "5002:5002"
    depends_on:
      - mydb